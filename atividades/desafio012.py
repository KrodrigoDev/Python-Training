precoProduto = float(input("Qual o valor do produto ? R$"))

desconto = precoProduto - (precoProduto * 5 / 100)

print(f'O produto com desconto de 5% vai custar R${desconto:.2f}')

import pandas as pd

